{
	"db": {
		"database": "$DB_NAME",
		"username": "$DB_USERNAME",
		"password": "$DB_PASSWORD",
		"host": "$DB_HOST",
		"charset": "utf8",
		"dialect": "$DB_DBMS_NAME",
		"port": "$DB_PORT",
		"define": {
			"underscored": false,
			"timestamps": true,
			"freezeTableName": false
		}
	}
}
